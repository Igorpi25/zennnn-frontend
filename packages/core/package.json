{
  "name": "@zennnn/core",
  "version": "0.36.7",
  "private": true,
  "main": "lib/index.js",
  "module": "lib/index.js",
  "files": [
    "src/",
    "lib/",
    "types/"
  ],
  "scripts": {
    "dev": "vite",
    "build": "yarn build:examples && vite build && yarn build:lib",
    "build:examples": "node ./build/gen-examples.js",
    "build:lib": "rm -rf ./lib/* && babel src --out-dir lib --source-maps",
    "test": "jest",
    "lint": "eslint src -f codeframe"
  },
  "dependencies": {
    "@zennnn/icons": "^0.36.7"
  }
}
