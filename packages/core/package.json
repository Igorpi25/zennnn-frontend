{
  "name": "@zennnn/core",
  "version": "0.36.7",
  "private": true,
  "main": "lib/index.cjs.js",
  "module": "lib/index.esm.js",
  "types": "lib/index.d.ts",
  "files": [
    "src/",
    "lib/",
    "types/"
  ],
  "scripts": {
    "build": "rm -rf ./lib/* && yarn build:lib && yarn build:bundle",
    "build:lib": "tsc -p tsconfig.dist.json",
    "build:bundle": "rollup -c",
    "gen:examples": "node ./scripts/generate-examples.js",
    "gen:api": "node ./scripts/generate-api.js",
    "test": "jest",
    "lint": "eslint src -f codeframe",
    "playground": "vite playground --port 4040",
    "playground:build": "vite build playground",
    "docs-dev": "node docs/server docs --port 3030",
    "docs-build": "yarn gen:examples && yarn gen:api && yarn docs-build:client && yarn docs-build:server",
    "docs-build:client": "vite build docs --ssrManifest --outDir dist/client",
    "docs-build:server": "vite build docs --ssr src/entry-server.js --outDir dist/server",
    "docs-generate": "vite build docs --ssrManifest --outDir dist/static && yarn docs-build:server && node docs/prerender",
    "docs-serve": "cross-env NODE_ENV=production node docs/server --port 5050"
  },
  "dependencies": {
    "@zennnn/icons": "^0.36.7"
  },
  "devDependencies": {
    "vitepress": "^0.14.1"
  }
}
